""Not yet complete""

- Declaring dependency:
implementation("androidx.recyclerview:recyclerview:1.2.1")

- Add RecyclerView to activity:
for example activity_main.xml

- Create elements layout:
create a new layout resource file in layout

- Design the layout of items:
design resource and set its height to wrap content

- Enable View Binding:
https://github.com/Shahnazi2002/Android-Kotlin-Samples/blob/main/ViewBinding.kt

- Set id for recycler view:
android:id="@+id/recyclerView"

- Set layout manager of recycler view in MainActivity.kt:
binding.recyclerView.layoutManager = LinearLayoutManager(this)

- Create adapter class file:
for example MyAdapter.kt

- Create data class:
for example DataClass.kt
